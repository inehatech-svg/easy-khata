/* Easy Khata configuration */
// Put YOUR Google OAuth Web Client ID here to enable "Continue with Google"
// (Google Cloud Console → Credentials → OAuth client ID → Web application,
//  add your site origin, e.g. https://your-project.web.app)
// It can also be set later inside the app: Settings → Backup & global sync.
window.EK_GOOGLE_CLIENT_ID = '';
